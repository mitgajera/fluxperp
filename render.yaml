# Render Blueprint — deploys the FluxPerp data services as one always-on worker.
# Render → New → Blueprint → point at this repo. Then set WALLET_SECRET (deployer keypair
# JSON array) as a secret env var. The frontend deploys separately on Vercel (root dir = app/).
services:
  # Free web service (workers are paid on Render). It exposes a health port; keep it awake
  # with a free uptime pinger (UptimeRobot / cron-job.org) hitting the URL every ~10 min so
  # the free instance never sleeps and the data services keep running 24/7.
  - type: web
    name: fluxperp-services
    runtime: docker
    dockerfilePath: ./Dockerfile
    plan: free
    autoDeploy: true
    healthCheckPath: /
    envVars:
      - key: WALLET_SECRET
        sync: false # set this as a secret in the dashboard
      - key: MARKETS
        value: "0,1"
      - key: SOLANA_L1_RPC
        value: https://api.devnet.solana.com
      - key: ER_RPC
        value: https://devnet-as.magicblock.app
      - key: ER_WS
        value: wss://devnet-as.magicblock.app
